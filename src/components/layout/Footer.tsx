const FooterStrip = () => {
    return (
        <footer className="footer-strip">
            <p>
                Built with focus on performance & clean systems — © {new Date().getFullYear()} Vastav
            </p>
        </footer>
    );
};

export default FooterStrip;
